Generates sheet music in a random fashion for practicing sight reading.

It uses a random number generator when picking the note values, pitches or chunks of notes so that new notes are generated in each call, thus preventing learning them by memory.

http://achtelbass.de/
